#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    long long int A;
    long long int B;
    scanf("%lld %lld", &A, &B);
    long long int  multi = A * B;
    printf("%lld", multi);
    return 0;
}
